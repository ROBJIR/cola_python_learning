![Coders-Lab-1920px-no-background](https://user-images.githubusercontent.com/30623667/104709394-2cabee80-571f-11eb-9518-ea6a794e558e.png)


## Exercise 1 &ndash; done with the lecturer

Create a new table named `ClientAddress` that contains:

* city: string
* street: string
* house_no: string

The `ClientAddress` table must be connected by a one-to-one relationship with the `Clients` table. Write several SQL queries that will enter addresses for existing users.

Write the queries in the file **exercise1.py**.

---


## Exercise 3

Create a payment table. It should have the same data as in the exercises from the previous day, but it should be related to the `Ticket` table by a one-to-one relationship (this will affect the **id** column). The relation between ticket and payment is as follows: the ticket has 1 or 0 payments (it is then unpaid) &ndash; payment must be assigned to a ticket.

Write the queries in the **exercise2.py** file.
